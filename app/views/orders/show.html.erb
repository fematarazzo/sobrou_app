<div class="container mt-4">


    <div class="card-border ml-2 mt-2">
      <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @order.dish.photo.key %>')">
      </div>
      <p class="text-center card-fill"><%= @order.dish.name%></p>
    </div>


    <div class="card-border ml-2 p-2">
      <p><strong>Detalhes da reserva</strong></p>
      <p><%= @order.dish.description%></p>
      <p> R$ <%= @order.dish.price %></p>
      <p>Horário de retirada: <%= @order.dish.pickup %></p>
    </div>

    <div class="card-border ml-2 p-2">
      <p><strong>Atenção</strong> Lembre-se que a variedade dos produtos será preestabelecida pelo colaborador de acordo com a disponibilidade de comida excedente.</p>
      <p>As imagens não correspondem, necessariamente, ao produto fornecido.</p>
    </div>

    <div class="card-border ml-2 mt-2 p-1">
      <p><strong><%= @order.dish.restaurant.name %></strong></p>
      <p> <%= @order.rating %></p>
      <p><%= @order.dish.restaurant.address%></p>
      <p><%= @order.dish.restaurant.phone%></p>
    </div>

    <div class="text-center">
      <%= link_to "Voltar para Home", root_path, class:"btn btn-success mt-2" %>
    </div>

  <p><%= cl_image_tag @order.dish.photo.key,class:"card-img-top mt-2"%>
  </p>
  <div>
    <p><strong>O que tenho a chance de salvar?</strong></p>
    <p><%= @order.dish.description%></p>
    <p> R$ <%= @order.dish.price %></p>

    <p>Data:</p>
    <p>Horário de retirada: </p>

    <p>Atenção: Lembre-se que a variedade dos produtos será preestabelecida pelo colaborador de acordo com a disponibilidade de comida excedente</p>
    <p>As imagens não correspondem, necessariamente, ao produto fornecido.</p>

    <p><strong><%= @order.dish.restaurant.name %></strong></p>

    <p> <%= @order.rating %></p>
    <p><%= @order.dish.restaurant.address%></p>
    <p><%= @order.dish.restaurant.phone%></p>
  </div>
  <%= link_to "Confirmar e voltar para Home", root_path, class:"btn btn-success" %>

</div>


